# Vercel Deployment & Environment Variables

# The absolute URL where the application runs
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Enterprise KMS Secret for AES-256-GCM encryption of explicit AI integrations
# MUST be 32 raw characters (256-bit) minimum for production security.
ENCRYPTION_SECRET="your-32-byte-enterprise-secret-key-do-not-leak"

# Production PostgreSQL Connection Target
# Required: Provide a valid Postgres URI (e.g. Supabase, Neon, AWS RDS)
DATABASE_URL="postgresql://user:password@localhost:5432/toastops?schema=public"

# Note: We do not use next-auth, clerk, or specific Vercel tokens since the platform is entirely DIY.
